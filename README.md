## Запуск игры
  1. Клонируйте репозиторий.
  2. Перейдите в дирекотрию проекта.
  3. Создайте и активируйте виртуальное окружение, установите зависимости:
     ```
     python -m venv venv
     
     source venv/bin/activate (Linux)
     или
     source venv/Scripts/activate (Windows)
     
     pip install -r requirements.txt
     ```
  4. Установите шрифты из папки fonts.
  5. Запустите игру через терминал ```python the_snake.py``` или IDE.
     
# The Snake V 1.0
### Описание
Python ООП реализация классической игры **"Змейка" - the snake** на библиотке ![Static Badge](https://img.shields.io/badge/pygame-b784a7?label=2.5.2).

<img width="300" alt="image" src="https://github.com/user-attachments/assets/145c2c5b-8bd3-4be3-a8a5-6da1067abe53" />

Игровое поле размером 640х480 поделено на клетки 20х20.
Реализована логика при поедании яблок и столкновении головы змейки с её сегментом. Графический интерфейс написан на библиотеке Pygame.

## Правила игры:
- Змейка состоит из сегментов.
- Используются стрелочные клавиши **(↑, ↓, →, ←)** для управления направлением движения змейки.
- Когда игрок нажимает клавишу направления, змейка меняет своё движение соответственно направлению нажатия. Важно учитывать, что нельзя мгновенно повернуть обратно (например, если двигаетесь вверх, сразу вниз двигаться нельзя).
- 🍎 появляется случайно на свободном месте карты.
- Каждый раз, когда змейка съедает яблоко, она увеличивается в длину на один сегмент.
- В данной вариации змейка может проходить сквозь одну стену и появляться с противоположной стороны поля.
- Если змейка столкнётся сама с собой — игра начинается с начала.

## Классы игровых объектов:
- **GameObject** — это базовый класс, от которого наследуются другие игровые объекты. Он содержит общие атрибуты игровых объектов — например, эти атрибуты описывают позицию и цвет объекта. Этот же класс содержит и заготовку метода для отрисовки объекта на игровом поле — draw.
- **Snake** — класс, унаследованный от GameObject, описывающий змейку и её поведение. Этот класс управляет её движением, отрисовкой, а также обрабатывает действия пользователя.
- **Apple** — класс, унаследованный от GameObject, описывающий яблоко и действия с ним. Яблоко должно отображаться в случайных клетках игрового поля.

## Итог
Игра демонстрирует базовую реализацию механики классического аркадного жанра "Snake". Код легко расширяется дополнительными элементами, такими как бонусы, препятствия, усложнения уровней и улучшенная графика.

# The Snake V 2.0
Добавлено:
- экран паузы(клавиша p)
- экран после поражения
- яблоко постепенно исчезает
- счетчик очков
- более лаконичный код в main
- добавлен класс GameState для хранения состояния игры
- константы пересены в отдельный файл

![the_snake animation](https://github.com/user-attachments/assets/c9e65a07-be95-4bdf-8ab6-eba8d1446380)
